# Отчет по выполнению практической работы №4

### Автор: Фёдоров Антон Сергеевич
### Группа: БСБО-09-23

---

## Цель работы

Целью данной работы являлось изучение основ многопоточности в Android, различий между процессом и потоком, а также освоение механизмов запуска фоновых потоков и передачи данных в главный UI-поток приложения.

## Структура работы

Практическая работа №4 посвящена базовым средствам многопоточности и включает следующие логические части:

- **Основные понятия:** процесс, поток, состояния потока, главный поток приложения.
- **Создание потоков:** использование `Thread` и `Runnable`.
- **Работа с UI-потоком:** безопасное обновление интерфейса из фонового потока.
- **Передача данных между потоками:** `runOnUiThread`, `View.post`, `Handler`, `Looper`, `MessageQueue`.
- **Пул потоков:** применение `Executor`.

---

## Выполненные задания

### Задание 1. Изучение процессов и потоков

**Задача:** Рассмотреть различия между процессом и потоком в Android и изучить состояния потока.

В ходе выполнения задания было установлено, что процесс является изолированным экземпляром приложения, которому выделяются системные ресурсы, тогда как поток представляет собой способ выполнения кода внутри процесса. Также были изучены состояния потока: `New`, `Runnable`, `Blocked`, `Waiting` и `Terminated`.

### Задание 2. Создание потока с помощью `Thread`

**Задача:** Создать отдельный поток выполнения и запустить его.

**Пример кода:**

```java
public class MyThread extends Thread {
    @Override
    public void run() {
        Log.d("MyThread", "Мой поток запущен");
    }
}

MyThread myThread = new MyThread();
myThread.start();
```

### Задание 3. Создание потока с помощью `Runnable`

**Задача:** Реализовать альтернативный способ создания фонового потока через интерфейс `Runnable`.

**Пример кода:**

```java
Runnable runnable = new Runnable() {
    @Override
    public void run() {
        Log.d("RunnableDemo", "Поток на базе Runnable запущен");
    }
};

new Thread(runnable).start();
```

### Задание 4. Передача данных в UI-поток

**Задача:** Обновить элементы интерфейса из фонового потока безопасным способом.

В Android запрещено изменять элементы интерфейса напрямую из не-UI-потока, поэтому были рассмотрены методы:

- `runOnUiThread(Runnable)`;
- `View.post(Runnable)`;
- `View.postDelayed(Runnable, long)`.

**Пример кода:**

```java
new Thread(() -> {
    try {
        Thread.sleep(2000);
        runOnUiThread(() -> textView.setText("Обновление из фонового потока"));
    } catch (InterruptedException e) {
        e.printStackTrace();
    }
}).start();
```

### Задание 5. Использование `Handler` и `Looper`

**Задача:** Организовать передачу сообщений между потоками через очередь сообщений.

**Пример кода:**

```java
Handler handler = new Handler(Looper.getMainLooper()) {
    @Override
    public void handleMessage(@NonNull Message msg) {
        textView.setText("Номер итерации: " + msg.what);
    }
};
```

Данный механизм позволяет передавать сообщения из фонового потока в главный поток и обрабатывать их в UI без нарушения правил Android.

### Задание 6. Использование `Executor`

**Задача:** Изучить способ запуска набора задач через пул потоков.

**Пример кода:**

```java
Executor executor = Executors.newFixedThreadPool(4);
executor.execute(() -> Log.d("ExecutorDemo", "Задача выполняется в пуле потоков"));
```

---

## Выводы

В ходе выполнения практической работы №4 были изучены базовые принципы многопоточности в Android. Были рассмотрены различия между процессом и потоком, способы создания фоновых потоков, а также методы передачи данных в главный поток приложения. На практике были освоены `Thread`, `Runnable`, `Handler`, `Looper`, `MessageQueue` и `Executor`. Полученные знания позволяют строить приложения, в которых длительные операции не блокируют интерфейс пользователя.
